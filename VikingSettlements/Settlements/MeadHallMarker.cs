using UnityEngine;

namespace VikingSettlements.Settlements
{
    /// <summary>
    /// Marks a settlement's mead hall: the VS_HallBanner piece the mead hall
    /// blueprint raises carries this, and the Innkeeper job gates on one
    /// being inside the settlement radius (the same way a forge gates the
    /// blacksmith). No behavior of its own.
    /// </summary>
    public class MeadHallMarker : MonoBehaviour
    {
    }
}
